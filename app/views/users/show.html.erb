<h1> Welcome, <%= @user.name %>! </h1>
<%= link_to "New Group", new_user_group_path(@user.id)%>
<%= link_to "Join a group", groups_addgroup_path %>

<br>
<h3>Your Groups:</h3>
<% @user.groups.each do |t| %> <br>
    <%= link_to t.name, group_path(t.id) %><br>
<% end %>    
<br>

<%= link_to "Log Out", session_path, :method => :delete %>

<p> link to my saved recipes </p>