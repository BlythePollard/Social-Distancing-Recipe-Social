<h1> Welcome, <%= @user.name %>! </h1>
<p> <%= link_to "Create a group", new_user_group_path(@user.id)%> </p>
<p> <%= link_to "Join a group", groups_addgroup_path %> </p>
<p> <%= link_to "My saved recipes", user_saved_recipes_path(@user.id) %> </p>

<br>
<h3>Your Groups:</h3>
<% @user.groups.each do |t| %> <br>
    <%= link_to t.name, group_path(t.id) %><br>
<% end %>    
<br>

<%= link_to "Log Out", session_path, :method => :delete %>